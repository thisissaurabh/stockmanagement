import 'package:flutter/material.dart';

class ViewAllStock extends StatelessWidget {
  const ViewAllStock({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.red,
    );
  }
}
